define(['backbone', 'jquery', 'underscore'],
  function(Backbone, $, _) {
    var Bonus = Backbone.Model.extend({
      initialize: function() {

      },
      defaults: {

      }
    });

    return Bonus;
  }
);
